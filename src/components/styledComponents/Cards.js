import styled from "styled-components";

const Cards = styled.div`
  display: grid;
  grid-template-columns: 1fr 1fr;
  grid-gap: 5%;
  align-items: center;
  justify-items: center;
  border: 1px solid gold;
  box-shadow: inset 0 0 5px 3px gold;
`;

export default Cards;
